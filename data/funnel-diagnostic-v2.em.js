// Generated by EventMath Compiler v0.4
// Runtime: <script src="eventmath-runtime.js"></script>

const EM = typeof EventMathRuntime !== 'undefined'
  ? EventMathRuntime
  : require('../runtime/eventmath-runtime.js');

const __weights = {};
const __assumptions = [];

let healthcare_funnel = null;
let ecommerce_funnel = null;
let saas_funnel = null;
let coaching_funnel = null;
let education_funnel = null;
let headline_selector = null;
let page_selector = null;

// note: FUNNEL DIAGNOSTIC ENGINE v2 — EventMath v2.3
// note: Every condition passes through directions x lenses x quantities
// note: ── DIRECTIONS (how to approach) ──
// Event: "direct direction"
const direct_direction = new EM.EventMathEvent(
  "direct direction",
  "compass",
  {}
);

// Event: "indirect opposite"
const indirect_opposite = new EM.EventMathEvent(
  "indirect opposite",
  "compass",
  {}
);

// Event: "indirect other"
const indirect_other = new EM.EventMathEvent(
  "indirect other",
  "compass",
  {}
);

// Event: "keep same"
const keep_same = new EM.EventMathEvent(
  "keep same",
  "compass",
  {}
);

// Event: "more same"
const more_same = new EM.EventMathEvent(
  "more same",
  "compass",
  {}
);

// Event: "less same"
const less_same = new EM.EventMathEvent(
  "less same",
  "compass",
  {}
);

// Layer: "directions"
const directions = new EM.EventMathLayer("directions", [
  direct_direction,
  indirect_opposite,
  indirect_other,
  keep_same,
  more_same,
  less_same,
]);

// note: ── LENSES (which dimension) ──
// Event: "who lens"
const who_lens = new EM.EventMathEvent(
  "who lens",
  "lens",
  {}
);

// Event: "what lens"
const what_lens = new EM.EventMathEvent(
  "what lens",
  "lens",
  {}
);

// Event: "place lens"
const place_lens = new EM.EventMathEvent(
  "place lens",
  "lens",
  {}
);

// Event: "time lens"
const time_lens = new EM.EventMathEvent(
  "time lens",
  "lens",
  {}
);

// Event: "why lens"
const why_lens = new EM.EventMathEvent(
  "why lens",
  "lens",
  {}
);

// Event: "how lens"
const how_lens = new EM.EventMathEvent(
  "how lens",
  "lens",
  {}
);

// Layer: "lenses"
const lenses = new EM.EventMathLayer("lenses", [
  who_lens,
  what_lens,
  place_lens,
  time_lens,
  why_lens,
  how_lens,
]);

// note: ── QUANTITIES (how much) ──
// Event: "small quantity"
const small_quantity = new EM.EventMathEvent(
  "small quantity",
  "quantity",
  {}
);

// Event: "medium quantity"
const medium_quantity = new EM.EventMathEvent(
  "medium quantity",
  "quantity",
  {}
);

// Event: "large quantity"
const large_quantity = new EM.EventMathEvent(
  "large quantity",
  "quantity",
  {}
);

// Event: "very large quantity"
const very_large_quantity = new EM.EventMathEvent(
  "very large quantity",
  "quantity",
  {}
);

// Layer: "quantities"
const quantities = new EM.EventMathLayer("quantities", [
  small_quantity,
  medium_quantity,
  large_quantity,
  very_large_quantity,
]);

// note: ── INDUSTRIES ──
// Event: "healthcare"
const healthcare = new EM.EventMathEvent(
  "healthcare",
  "industry",
  {}
);

// Event: "ecommerce"
const ecommerce = new EM.EventMathEvent(
  "ecommerce",
  "industry",
  {}
);

// Event: "saas"
const saas = new EM.EventMathEvent(
  "saas",
  "industry",
  {}
);

// Event: "coaching"
const coaching = new EM.EventMathEvent(
  "coaching",
  "industry",
  {}
);

// Event: "education"
const education = new EM.EventMathEvent(
  "education",
  "industry",
  {}
);

// Layer: "industries"
const industries = new EM.EventMathLayer("industries", [
  healthcare,
  ecommerce,
  saas,
  coaching,
  education,
]);

// note: ── PRICE TIERS ──
// Event: "free tier"
const free_tier = new EM.EventMathEvent(
  "free tier",
  "price",
  {}
);

// Event: "low tier"
const low_tier = new EM.EventMathEvent(
  "low tier",
  "price",
  {}
);

// Event: "medium tier"
const medium_tier = new EM.EventMathEvent(
  "medium tier",
  "price",
  {}
);

// Event: "high tier"
const high_tier = new EM.EventMathEvent(
  "high tier",
  "price",
  {}
);

// Event: "premium tier"
const premium_tier = new EM.EventMathEvent(
  "premium tier",
  "price",
  {}
);

// Layer: "prices"
const prices = new EM.EventMathLayer("prices", [
  free_tier,
  low_tier,
  medium_tier,
  high_tier,
  premium_tier,
]);

// note: ── AWARENESS LEVELS ──
// Event: "unaware"
const unaware = new EM.EventMathEvent(
  "unaware",
  "awareness",
  {}
);

// Event: "problem aware"
const problem_aware = new EM.EventMathEvent(
  "problem aware",
  "awareness",
  {}
);

// Event: "solution aware"
const solution_aware = new EM.EventMathEvent(
  "solution aware",
  "awareness",
  {}
);

// Event: "product aware"
const product_aware = new EM.EventMathEvent(
  "product aware",
  "awareness",
  {}
);

// Layer: "awareness levels"
const awareness_levels = new EM.EventMathLayer("awareness levels", [
  unaware,
  problem_aware,
  solution_aware,
  product_aware,
]);

// note: ── FUNNEL TYPES (output) ──
// Event: "tripwire"
const tripwire = new EM.EventMathEvent(
  "tripwire",
  "funnel type",
  {}
);

// Event: "application"
const application = new EM.EventMathEvent(
  "application",
  "funnel type",
  {}
);

// Event: "summit"
const summit = new EM.EventMathEvent(
  "summit",
  "funnel type",
  {}
);

// Event: "webinar"
const webinar = new EM.EventMathEvent(
  "webinar",
  "funnel type",
  {}
);

// Event: "subscription"
const subscription = new EM.EventMathEvent(
  "subscription",
  "funnel type",
  {}
);

// Event: "quiz"
const quiz = new EM.EventMathEvent(
  "quiz",
  "funnel type",
  {}
);

// Layer: "funnel types"
const funnel_types = new EM.EventMathLayer("funnel types", [
  tripwire,
  application,
  summit,
  webinar,
  subscription,
  quiz,
]);

// note: ── CHAIN ANALYSIS ──
// note: Each industry passes through 3-axis filter to generate funnel recommendations
// Chain: "healthcare funnel"
healthcare_funnel = new EM.EventMathChain('healthcare funnel');
healthcare_funnel.addLink('healthcare', 'application funnel', 90);
healthcare_funnel.addLink('healthcare', 'quiz funnel', 10);

// Chain: "ecommerce funnel"
ecommerce_funnel = new EM.EventMathChain('ecommerce funnel');
ecommerce_funnel.addLink('ecommerce', 'tripwire funnel', 70);
ecommerce_funnel.addLink('ecommerce', 'quiz funnel', 30);

// Chain: "saas funnel"
saas_funnel = new EM.EventMathChain('saas funnel');
saas_funnel.addLink('saas', 'subscription funnel', 60);
saas_funnel.addLink('saas', 'webinar funnel', 40);

// Chain: "coaching funnel"
coaching_funnel = new EM.EventMathChain('coaching funnel');
coaching_funnel.addLink('coaching', 'application funnel', 40);
coaching_funnel.addLink('coaching', 'webinar funnel', 30);
coaching_funnel.addLink('coaching', 'subscription funnel', 20);
coaching_funnel.addLink('coaching', 'quiz funnel', 10);

// Chain: "education funnel"
education_funnel = new EM.EventMathChain('education funnel');
education_funnel.addLink('education', 'summit funnel', 50);
education_funnel.addLink('education', 'webinar funnel', 30);
education_funnel.addLink('education', 'subscription funnel', 20);

// note: ── ELEMENT SELECTION BY AWARENESS ──
// note: Pass awareness through 3-axis filter to select headline type
// Chain: "headline selector"
headline_selector = new EM.EventMathChain('headline selector');
headline_selector.addLink('problem aware', 'pain headline', 85);
headline_selector.addLink('problem aware', 'question headline', 15);
headline_selector.addLink('solution aware', 'benefit headline', 70);
headline_selector.addLink('solution aware', 'comparison headline', 30);
headline_selector.addLink('product aware', 'social proof headline', 60);
headline_selector.addLink('product aware', 'feature headline', 40);
headline_selector.addLink('unaware', 'curiosity headline', 70);
headline_selector.addLink('unaware', 'pain headline', 30);

// note: ── PAGE SELECTION BY PRICE ──
// note: Pass price through 3-axis filter to select page count and complexity
// Chain: "page selector"
page_selector = new EM.EventMathChain('page selector');
page_selector.addLink('free tier', 'minimal', 80);
page_selector.addLink('free tier', 'standard', 20);
page_selector.addLink('low tier', 'standard', 60);
page_selector.addLink('low tier', 'expanded', 40);
page_selector.addLink('medium tier', 'expanded', 50);
page_selector.addLink('medium tier', 'standard', 30);
page_selector.addLink('medium tier', 'full', 20);
page_selector.addLink('high tier', 'expanded', 40);
page_selector.addLink('high tier', 'full', 40);
page_selector.addLink('high tier', 'standard', 20);
page_selector.addLink('premium tier', 'full', 60);
page_selector.addLink('premium tier', 'expanded', 40);

// note: ── GENERATE ALL COMBOS ──
// note: Each dimension passes through the 3-axis filter
// note: 5 industries x 5 prices x 4 awareness = 100 predictions
// predict: "recommended funnel" → all recommendations
const all_recommendations = new EM.EventMathLayer('all recommendations', []);
industries.events.forEach(_dir => {
  prices.events.forEach(_lens => {
    awareness_levels.events.forEach(_qty => {
      all_recommendations.events.push(new EM.EventMathEvent(
        'prediction_' + Date.now() + '_' + Math.random().toString(36).slice(2),
        'prediction',
        {
          subject: 'recommended funnel',
          direction: _dir.matter ? _dir.matter.name : String(_dir.id),
          lens: _lens.matter ? _lens.matter.name : String(_lens.id),
          quantity: _qty.matter ? _qty.matter.name : String(_qty.id),
          predicted_state: (_dir.matter ? _dir.matter.name : '') + ' change through ' + (_lens.matter ? _lens.matter.name : '') + ' lens affecting ' + (_qty.matter ? _qty.matter.name : ''),
          confidence: 0.5,
          resolved: false,
          correct: null
        }
      ));
    });
  });
});

console.log(all_recommendations.render());
// note: ── SHOW ANALYSIS ──
{ const __sv = healthcare_funnel; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('healthcare funnel:', __sv); } }
{ const __sv = ecommerce_funnel; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('ecommerce funnel:', __sv); } }
{ const __sv = saas_funnel; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('saas funnel:', __sv); } }
{ const __sv = coaching_funnel; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('coaching funnel:', __sv); } }
{ const __sv = education_funnel; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('education funnel:', __sv); } }
{ const __sv = headline_selector; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('headline selector:', __sv); } }
{ const __sv = page_selector; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('page selector:', __sv); } }

// ── Export for use ─────────────────────────────────
module.exports = {
  direct_direction,
  indirect_opposite,
  indirect_other,
  keep_same,
  more_same,
  less_same,
  who_lens,
  what_lens,
  place_lens,
  time_lens,
  why_lens,
  how_lens,
  small_quantity,
  medium_quantity,
  large_quantity,
  very_large_quantity,
  healthcare,
  ecommerce,
  saas,
  coaching,
  education,
  free_tier,
  low_tier,
  medium_tier,
  high_tier,
  premium_tier,
  unaware,
  problem_aware,
  solution_aware,
  product_aware,
  tripwire,
  application,
  summit,
  webinar,
  subscription,
  quiz,
  directions,
  lenses,
  quantities,
  industries,
  prices,
  awareness_levels,
  funnel_types,
  all_recommendations,
};